{{!
    @template local_jobboard/dashboard_widget

    Dashboard statistics widget template.

    Context variables required for this template:
    * title - Widget title
    * value - Main value to display
    * subtitle - Optional subtitle
    * icon - Icon name
    * color - Color class (primary, success, warning, danger, info)
    * url - Optional link URL

    Example context (json):
    {
        "title": "Active Vacancies",
        "value": 12,
        "subtitle": "3 closing soon",
        "icon": "briefcase",
        "color": "primary",
        "url": "/local/jobboard/index.php?view=vacancies"
    }
}}
<div class="jb-card jobboard-widget jb-border-{{color}} jb-h-100 jb-card-shadow">
    <div class="jb-card-body">
        <div class="jb-d-flex jb-justify-content-between jb-align-items-start">
            <div>
                <h6 class="jb-card-subtitle jb-mb-2 jb-text-muted">{{title}}</h6>
                <h2 class="jb-card-title jb-mb-0 jb-text-{{color}}">
                    {{#url}}<a href="{{url}}" class="jb-text-{{color}} jb-text-decoration-none">{{/url}}
                    {{value}}
                    {{#url}}</a>{{/url}}
                </h2>
                {{#subtitle}}
                    <small class="jb-text-muted">{{subtitle}}</small>
                {{/subtitle}}
            </div>
            <div class="widget-icon jb-bg-{{color}} jb-text-white jb-rounded-circle jb-p-3">
                {{#pix}}i/{{icon}}, core{{/pix}}
            </div>
        </div>
    </div>
    {{#url}}
        <div class="jb-card-footer jb-bg-transparent jb-border-{{color}}">
            <a href="{{url}}" class="jb-small jb-text-{{color}}">
                {{#str}}view, local_jobboard{{/str}} {{#pix}}i/next, core{{/pix}}
            </a>
        </div>
    {{/url}}
</div>
