{{!
    @template local_jobboard/components/status_badge

    Status badge component with accessibility support.

    Context variables:
    * status - Status code (used for aria-label context)
    * label - Display label
    * color - Bootstrap color (primary, success, warning, danger, info, secondary)
    * icon - Optional FontAwesome icon
    * size - Optional size (sm, lg)
    * pill - Boolean for pill style

    Example context (json):
    {
        "status": "published",
        "label": "Published",
        "color": "success",
        "icon": "check-circle",
        "pill": true
    }
}}
<span class="badge badge-{{color}}{{#pill}} badge-pill{{/pill}}{{#size}} badge-{{size}}{{/size}}"
      role="status"
      aria-label="{{#str}}status{{/str}}: {{label}}">
    {{#icon}}
    <i class="fa fa-{{icon}} mr-1" aria-hidden="true"></i>
    {{/icon}}
    <span class="badge-text">{{label}}</span>
</span>
