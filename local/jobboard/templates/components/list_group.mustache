{{!
    @template local_jobboard/components/list_group

    List group component.

    Context variables:
    * items (array) - List items {label, value, icon, url, badge, active}
    * flush (bool) - Remove borders and rounded corners

    Example context (json):
    {
        "items": [
            {"label": "Item 1", "value": "Value", "icon": "file"},
            {"label": "Item 2", "badge": {"value": "5", "color": "primary"}}
        ],
        "flush": true
    }
}}
<ul class="list-group{{#flush}} list-group-flush{{/flush}}">
    {{#items}}
    {{#url}}
    <a href="{{url}}" class="list-group-item list-group-item-action{{#active}} jb-active{{/active}}">
    {{/url}}
    {{^url}}
    <li class="list-group-item{{#active}} jb-active{{/active}}">
    {{/url}}
        <div class="d-flex justify-content-between align-items-center">
            <span>
                {{#icon}}<i class="fa fa-{{icon}} me-2 text-muted" aria-hidden="true"></i>{{/icon}}
                {{label}}
            </span>
            {{#value}}<span class="text-muted">{{value}}</span>{{/value}}
            {{#badge}}<span class="badge jb-badge-{{badge.color}}{{#badge.pill}} rounded-pill{{/badge.pill}}">{{badge.value}}</span>{{/badge}}
        </div>
    {{#url}}
    </a>
    {{/url}}
    {{^url}}
    </li>
    {{/url}}
    {{/items}}
</ul>
