{{!
    @template local_jobboard/components/alert

    Alert component for displaying messages.

    Context variables:
    * type (string) - Alert type: success, danger, warning, info, primary
    * message (string) - Alert message content
    * icon (string) - Optional FontAwesome icon name
    * dismissible (bool) - Whether alert can be dismissed

    Example context (json):
    {
        "type": "success",
        "message": "Operation completed successfully",
        "icon": "check-circle",
        "dismissible": true
    }
}}
<div class="jb-alert jb-alert-{{type}}{{#dismissible}} jb-alert-dismissible{{/dismissible}}" role="alert">
    {{#icon}}<i class="fa fa-{{icon}} jb-mr-2" aria-hidden="true"></i>{{/icon}}
    {{{message}}}
    {{#dismissible}}
    <button type="button" class="jb-close" data-dismiss="alert" aria-label="{{#str}}close{{/str}}">
        <span aria-hidden="true">&times;</span>
    </button>
    {{/dismissible}}
</div>
