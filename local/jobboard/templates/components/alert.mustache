{{!
    @template local_jobboard/components/alert

    Alert component for displaying messages with enhanced UX.

    Context variables:
    * type (string) - Alert type: success, danger, warning, info, primary
    * message (string) - Alert message content (can be HTML)
    * title (string) - Optional alert title/heading
    * icon (string) - Optional FontAwesome icon name (auto-detected if not provided)
    * dismissible (bool) - Whether alert can be dismissed
    * animate (bool) - Enable entrance animation
    * compact (bool) - Use compact padding
    * actions (array) - Optional action buttons [{url, label, color}]
    * list (array) - Optional list of items to display

    Example context (json):
    {
        "type": "success",
        "title": "Success!",
        "message": "Operation completed successfully",
        "icon": "check-circle",
        "dismissible": true,
        "animate": true,
        "actions": [{"url": "/view", "label": "View Details", "color": "success"}]
    }
}}
<div class="alert jb-alert-{{type}}{{#dismissible}} jb-alert-dismissible{{/dismissible}}{{#animate}} jb-fade-in-down{{/animate}}{{#compact}} py-2 px-3{{/compact}}"
     role="alert"
     aria-live="{{#isdanger}}assertive{{/isdanger}}{{^isdanger}}polite{{/isdanger}}">

    <div class="d-flex align-items-start">
        {{! Icon - auto-detect based on type if not provided }}
        <div class="jb-alert-icon me-3 mt-1" aria-hidden="true">
            {{#icon}}
            <i class="fa fa-{{icon}} fa-lg"></i>
            {{/icon}}
            {{^icon}}
            {{#issuccess}}<i class="fa fa-circle-check fa-lg"></i>{{/issuccess}}
            {{#isdanger}}<i class="fa fa-circle-exclamation fa-lg"></i>{{/isdanger}}
            {{#iswarning}}<i class="fa fa-triangle-exclamation fa-lg"></i>{{/iswarning}}
            {{#isinfo}}<i class="fa fa-circle-info fa-lg"></i>{{/isinfo}}
            {{#isprimary}}<i class="fa fa-bell fa-lg"></i>{{/isprimary}}
            {{/icon}}
        </div>

        <div class="flex-grow-1">
            {{#title}}
            <h4 class="jb-alert-heading fw-bold mb-1">{{title}}</h4>
            {{/title}}

            <div class="jb-alert-content">
                {{{message}}}
            </div>

            {{#list}}
            <ul class="mb-0 mt-2 jb-pl-3">
                {{#items}}
                <li>{{.}}</li>
                {{/items}}
            </ul>
            {{/list}}

            {{#actions}}
            <div class="jb-alert-actions mt-3 d-flex gap-2 flex-wrap">
                {{#.}}
                <a href="{{url}}" class="btn btn-sm jb-btn-{{#color}}{{color}}{{/color}}{{^color}}outline-{{type}}{{/color}}">
                    {{#icon}}<i class="fa fa-{{icon}} me-1" aria-hidden="true"></i>{{/icon}}
                    {{label}}
                </a>
                {{/.}}
            </div>
            {{/actions}}
        </div>

        {{#dismissible}}
        <button type="button"
                class="jb-btn-close ms-2"
                data-dismiss="alert"
                aria-label="{{#str}}dismiss, local_jobboard{{/str}}">
            <i class="fa fa-xmark" aria-hidden="true"></i>
        </button>
        {{/dismissible}}
    </div>
</div>
