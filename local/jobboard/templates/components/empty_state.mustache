{{!
    @template local_jobboard/components/empty_state

    Empty state component for when no results found.

    Context variables:
    * message - Main message to display
    * icon - FontAwesome icon name (without fa- prefix)
    * description - Optional additional description
    * action - Optional action button {url, label, icon, class}

    Example context (json):
    {
        "message": "No vacancies found",
        "icon": "briefcase",
        "description": "Try adjusting your filters",
        "action": {"url": "/create", "label": "Create Vacancy", "icon": "plus", "class": "jb-btn-primary"}
    }
}}
<div class="jb-empty-state" role="status" aria-live="polite">
    <div class="jb-empty-state-icon" aria-hidden="true">
        <i class="fa fa-{{#icon}}{{icon}}{{/icon}}{{^icon}}inbox{{/icon}} fa-4x"></i>
    </div>
    <h4 class="jb-empty-state-title">{{message}}</h4>
    {{#description}}
    <p class="jb-empty-state-desc">{{description}}</p>
    {{/description}}
    {{#action}}
    <a href="{{url}}" class="jb-btn {{#class}}{{class}}{{/class}}{{^class}}jb-btn-primary{{/class}}">
        {{#icon}}
        <i class="fa fa-{{icon}} jb-mr-1" aria-hidden="true"></i>
        {{/icon}}
        {{label}}
    </a>
    {{/action}}
</div>
