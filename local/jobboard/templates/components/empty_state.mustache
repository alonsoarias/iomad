{{!
    @template local_jobboard/components/empty_state

    Empty state component for when no data is available.

    Context variables:
    * icon (string) - FontAwesome icon name
    * title (string) - Main message
    * description (string) - Optional secondary message
    * action (object) - Optional action button {url, label, color}

    Example context (json):
    {
        "icon": "folder-open",
        "title": "No items found",
        "description": "Try adjusting your filters",
        "action": {"url": "/create", "label": "Create New", "color": "primary"}
    }
}}
<div class="jb-empty-state jb-text-center jb-py-5" role="status">
    <i class="fa fa-{{icon}} fa-4x jb-text-muted jb-mb-3" aria-hidden="true"></i>
    <h3 class="jb-fs-5 jb-text-muted jb-mb-2">{{title}}</h3>
    {{#description}}
    <p class="jb-text-muted jb-mb-4">{{description}}</p>
    {{/description}}
    {{#action}}
    <a href="{{url}}" class="jb-btn jb-btn-{{color}}">
        {{#icon}}<i class="fa fa-{{icon}} jb-mr-2" aria-hidden="true"></i>{{/icon}}
        {{label}}
    </a>
    {{/action}}
</div>
