{{!
    @template local_jobboard/components/stat_card

    Statistics card component for displaying numeric values.

    Context variables:
    * value (string|number) - The statistic value
    * label (string) - Label describing the statistic
    * icon (string) - FontAwesome icon name
    * color (string) - Color theme: primary, success, warning, danger, info
    * url (string) - Optional link URL
    * trend (object) - Optional trend data {value, direction: up|down}

    Example context (json):
    {
        "value": "42",
        "label": "Applications",
        "icon": "file-alt",
        "color": "primary",
        "url": "/applications"
    }
}}
<div class="jb-card jb-card-shadow jb-stat-card jb-stat-card-{{color}}">
    {{#url}}<a href="{{url}}" class="jb-text-decoration-none jb-text-inherit">{{/url}}
    <div class="jb-card-body jb-d-flex jb-align-items-center">
        <div class="jb-stat-icon jb-mr-3">
            <i class="fa fa-{{icon}}" aria-hidden="true"></i>
        </div>
        <div class="jb-flex-grow-1">
            <div class="jb-stat-value jb-fs-3 jb-fw-bold">{{value}}</div>
            <div class="jb-stat-label jb-small jb-text-muted">{{label}}</div>
        </div>
        {{#trend}}
        <div class="jb-stat-trend jb-text-{{#isup}}success{{/isup}}{{^isup}}danger{{/isup}}">
            <i class="fa fa-arrow-{{direction}}" aria-hidden="true"></i>
            <small>{{value}}</small>
        </div>
        {{/trend}}
    </div>
    {{#url}}</a>{{/url}}
</div>
