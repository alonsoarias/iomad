{{!
    @template local_jobboard/pages/user/profile

    Update profile page for Job Board applicants.
    Uses jb-* CSS classes for consistent styling.
    Includes accessibility features, animations, and loading states.

    Context variables:
    * pagetitle (string) - Page title
    * intro (string) - Introduction text
    * hasvacancy (bool) - Whether applying for a specific vacancy
    * vacancy (object) - Vacancy info {title, code, location}
    * formhtml (string) - Rendered form HTML
    * dashboardurl (string) - URL to dashboard
    * loading (bool) - Show skeleton loading state
    * breadcrumbs (array) - Breadcrumb items

    Example context (json):
    {
        "pagetitle": "Update Profile",
        "intro": "Please update your profile...",
        "hasvacancy": true,
        "vacancy": {"title": "Professor", "code": "VAC-001"}
    }
}}
{{#loading}}
{{! Skeleton Loading State }}
<div class="local-jobboard-profile" aria-busy="true" aria-label="{{#str}}loading{{/str}}">
    <div class="jb-skeleton-text mb-3" style="width: 200px; height: 1em;"></div>
    <div class="mb-4">
        <div class="jb-skeleton-title mb-2" style="width: 60%;"></div>
        <div class="jb-skeleton-text" style="width: 80%;"></div>
    </div>
    <div class="card mb-4">
        <div class="card-header bg-light">
            <div class="jb-skeleton-text" style="width: 40%;"></div>
        </div>
        <div class="card-body">
            <div class="jb-skeleton-text mb-2" style="width: 70%;"></div>
            <div class="jb-skeleton-text" style="width: 50%;"></div>
        </div>
    </div>
    <div class="card">
        <div class="card-body">
            <div class="jb-skeleton-text mb-3" style="width: 30%; height: 2em;"></div>
            <div class="jb-skeleton-text mb-3" style="width: 100%; height: 40px;"></div>
            <div class="jb-skeleton-text mb-3" style="width: 100%; height: 40px;"></div>
            <div class="jb-skeleton-text mb-3" style="width: 100%; height: 40px;"></div>
        </div>
    </div>
</div>
{{/loading}}

{{^loading}}
<div class="local-jobboard-profile" data-region="jobboard-profile">
    {{! Skip Link for Accessibility }}
    <a href="#profile-main-content" class="jb-skip-link">
        {{#str}}skiptocontent, local_jobboard{{/str}}
    </a>

    {{! Page Header }}
    <header class="jb-page-header mb-4 jb-fade-in">
        <h1 class="fs-3 mb-2">
            <i class="fa fa-user-pen text-primary me-2" aria-hidden="true"></i>
            {{pagetitle}}
        </h1>
        {{#intro}}
        <p class="lead text-muted mb-0">{{intro}}</p>
        {{/intro}}
    </header>

    {{! Vacancy Info Card (if applying for a specific vacancy) }}
    {{#hasvacancy}}
    <div class="alert alert-info d-flex align-items-start mb-4 jb-fade-in-up" role="alert" style="animation-delay: 0.1s;">
        <div class="jb-alert-icon me-3">
            <i class="fa fa-briefcase fa-lg" aria-hidden="true"></i>
        </div>
        <div class="flex-grow-1">
            <h2 class="fs-6 mb-2 fw-bold">
                {{#str}}signup_applying_for, local_jobboard{{/str}}
            </h2>
            <p class="mb-1 fw-medium">{{vacancy.title}}</p>
            <div class="d-flex flex-wrap gap-3 small">
                <span>
                    <i class="fa fa-hashtag text-muted me-1" aria-hidden="true"></i>
                    <strong>{{vacancy.code}}</strong>
                </span>
                {{#vacancy.location}}
                <span>
                    <i class="fa fa-location-dot text-muted me-1" aria-hidden="true"></i>
                    {{vacancy.location}}
                </span>
                {{/vacancy.location}}
            </div>
        </div>
    </div>
    {{/hasvacancy}}

    {{! Profile Form }}
    <main id="profile-main-content">
        <div class="card shadow-sm jb-fade-in-up" style="animation-delay: 0.15s;">
            <div class="card-body">
                {{{formhtml}}}
            </div>
        </div>
    </main>

    {{! Navigation Footer }}
    <nav class="jb-navigation-footer d-flex justify-content-between align-items-center mt-4 py-3 border-top jb-fade-in" aria-label="{{#str}}pagenavigation, local_jobboard{{/str}}">
        <a href="{{dashboardurl}}" class="btn btn-outline-secondary">
            <i class="fa fa-arrow-left me-2" aria-hidden="true"></i>
            {{#str}}backtodashboard, local_jobboard{{/str}}
        </a>
    </nav>
</div>
{{/loading}}
